import { Typography } from "components/Typography";
import { Grid } from "components/Grid";
import styled from "styled-components";

export const StyledGrid = styled(Grid)`
  height: 48px;
`;

export const StyledNavigationLogo = styled(Typography)`
  * {
    margin: 0 auto;
  }
`;
